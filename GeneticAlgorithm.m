clear
clc

target = input('Target (string) : ','s');
population = input('Population (integer) : ');
mutation_rate = input('Mutation rate random(0-1) : ');

% Liblary Gentic Algorithms GA(taeget,populations,mutation)

GA(target,population,mutation_rate);
